<div class ="container">
    <div class="row text-center" id="top">
        <h1>EAT DA BURGER!</h1>
    </div>
        <div class="col-md-6 text-left">
            <h2>Burgers Remaining</h2>
                <ul>
                    {{#each burgers}}
                        {{#unless this.devoured}}
                        <h3>{{this.id}}. {{this.burger_name}}</h3>
                            <form method="POST" value={{this.id}}>
                                 
                                <button type="submit" class="btn btn-default">
                                </span>DEVOUR <span class="glyphicon glyphicon-menu-hamburger"></button>
                            </form>
                        {{/unless}}
                    {{/each}}
                </ul>
        </div>

        <div class="col-md-6 text-left"></div>
            <h2>Burgers Devoured</h2>
                <ul>
                    {{#each burgers}}
                        {{#if this.devoured}}
                        <h3>{{this.id}}. {{this.burger_name}} ✓</h3>
                        {{/if}}
                    {{/each}}
                </ul>
        </div>

        <div class= "container">
        <div class="col-md-3"></div>
            
        <div class="col-md-12 text-center">
            <h3>Hungry for a different burger? Enter one below!</h3>  
            <form class="addburger" action="/" method="POST">
                <input type="textbox" class="form-control col-md-4" name="newBurger" maxlength="100">
                <span class="addburger-btn">
                    <button type="submit" class="btn btn-default">ADD DA BURGER</button> 
                </span>
            </form>
        </div>
    </div>
</div>
